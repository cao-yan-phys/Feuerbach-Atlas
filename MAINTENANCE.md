# Maintenance

The static Vite site deploys to GitHub Pages from `main`.

- Production check: `npm run build`
- Test suite: `npm test -- --run`
- Runtime font: `public/fonts/OldStandard-Math.otf`
- Public source and runtime assets are self-contained.
